'use strict';

/**
 * promoted-category service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::promoted-category.promoted-category');
